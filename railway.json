{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "python manage.py migrate && python manage.py create_categories && gunicorn smartspend_backend.wsgi:application --bind 0.0.0.0:$PORT"
  }
}